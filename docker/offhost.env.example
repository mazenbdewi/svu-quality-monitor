# Copy OUTSIDE the repository to /etc/svu-quality-monitor/offhost.env (0600).
# No credentials or guessed destination. Fill only after provisioning your target.
RESTIC_REPOSITORY=
RESTIC_PASSWORD_FILE=
# Use a dedicated SSH config/identity outside the repo for an SFTP destination.
# Store the repository password separately/offline too; loss prevents recovery.
